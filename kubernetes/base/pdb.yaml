# Keeps at least one pod available during voluntary disruptions (node drains,
# cluster upgrades) — a key reliability control for SRE.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: demo-app
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: demo-app
